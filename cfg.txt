# 选择模型后端：
# transformers       = 使用 HF / Transformers 格式模型，读取 MODEL_PATH
# openai_compatible = 使用 GGUF / llama.cpp / Ollama / LM Studio 等外部模型服务，读取 LLM_API_BASE
MODEL_BACKEND=transformers
MODEL_PATH=C:\work\search\model\Qwen3.5-2B

# 如果使用 GGUF / llama.cpp，可把 MODEL_BACKEND 改成 openai_compatible，并填写下面路径。
LLAMA_SERVER_AUTO_START=1
LLAMA_SERVER_EXE=C:\tools\llama.cpp\llama-server.exe
GGUF_MODEL_PATH=C:\models\your-model.Q4_K_M.gguf
LLAMA_SERVER_PORT=8001
LLAMA_SERVER_CONTEXT=4096
LLAMA_SERVER_GPU_LAYERS=999
LLM_API_BASE=http://127.0.0.1:8001/v1/chat/completions
LLM_MODEL_NAME=local-model
LLM_TIMEOUT_SECONDS=120

SERVER_PORT=8080

# 默认先走本地，避免强制 Docker；需要联网时改成 AUTO 或 HYBRID。
APP_PROFILE=evidence
SEARCH_MODE=LOCAL
SEARCH_BACKEND=searxng
SEARXNG_URL=http://localhost:18080/search

# 本地知识库
LOCAL_KB_ENABLED=1
LOCAL_KB_DOCS_DIR=kb/documents
LOCAL_KB_DB_PATH=kb/kb.sqlite
LOCAL_KB_AUTO_SYNC=1
LOCAL_KB_SYNC_BEFORE_SEARCH=1
LOCAL_KB_SYNC_ON_START=0
LOCAL_KB_AUTO_SYNC_INTERVAL_SECONDS=30
LOCAL_KB_SEARCH_LIMIT=5

# 证据定位
LOCAL_EVIDENCE_ENABLED=1
SHOW_LOCAL_EVIDENCE_CHAIN=1
WEB_EVIDENCE_ENABLED=1
SHOW_WEB_EVIDENCE_CHAIN=1
WEB_EVIDENCE_MAX_TOTAL=8
WEB_EVIDENCE_PER_SOURCE=2
REQUIRE_EVIDENCE_CITATIONS=1
# 只想看证据、不调用模型时改成 1
EVIDENCE_ONLY_MODE=0

# 题库/答案直抽
DIRECT_ANSWER_ENABLED=1

# 回答风格：科研查证建议 strict；普通搜索可以改成 plain。
ANSWER_STYLE=strict
SHOW_EVIDENCE_AUDIT=1

# 并行搜索/抓取
PARALLEL_SEARCH_ENABLED=1
SEARCH_WORKERS=4
FETCH_WORKERS=4
FETCH_TIMEOUT_SECONDS=3
FAST_FETCH_TOP_N=1
PRECISE_FETCH_TOP_N=3
QUERY_TEMPLATE_FAST_LIMIT=1

# 输出长度
FAST_FINAL_TOKENS=384
PRECISE_FINAL_TOKENS=768
